#include "../includes/simulation.h"

#include <math.h>
#include <time.h>
#include <stdio.h>

int main()
{

    // Run similation
    runSimulation(0,20,0.1);
/*     outputV_init(2,2);
    outputV_wait(1);
    outputV_destroy(); */

    return 0;

}